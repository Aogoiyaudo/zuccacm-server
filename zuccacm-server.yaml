LogConfig:
  # Log Level, debug | info | warn | error | panic (default is info)
  Level: "info"
  # Log Path (default is Stdout)
  Path: "Stdout"

ServerConfig:
  # Port, serve port
  Port: 9000

Secret:
  # SSO Session Key
  SessionKey: "mainsite-session"
  # SSO Login API URL
  SSO_URL: "https://api.zuccacm.top/sso/v1/session"
  # DB
  DBConfig:
    Host: "localhost"
    Port: "3306"
    Database: "zuccacm"
    User: "root"
    Pwd: "123456"
  # MQ address, format like '127.0.0.1:9999'
  MessageQueue: ""